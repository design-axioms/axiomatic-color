# Validity Grid

Every surface × token combination, with achieved contrast.

<ValidityGrid />

Click a surface to see its tokens. Click a token to see how it performs across surfaces. Toggle atmosphere to see how color affects contrast.
